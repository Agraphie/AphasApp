import 'package:flutter/material.dart';

class MainCard extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Expanded(
        child: Card(
          color: Color.fromARGB(255, 212, 238, 255),
          margin: EdgeInsets.all(8.0),
          elevation: 2.0,
          child: Column(),
        ),
      );
}
